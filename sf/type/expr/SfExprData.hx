package sf.type.expr;

/**
 * For the time being we're just going to store the original TypedExpr
 * for purposes of pulling out position/type information out of it.
 * @author YellowAfterlife
 */
typedef SfExprData = haxe.macro.Type.TypedExpr;
